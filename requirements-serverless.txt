# RunPod Serverless SDK (REQUIRED for serverless deployment)
runpod>=1.7.0

# PyTorch with CUDA support for NVIDIA GPUs on RunPod
# Using CUDA 12.1 (compatible with CUDA 12.8.1 runtime)
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.6.0+cu121
torchvision==0.19.0+cu121
torchaudio==2.6.0+cu121

# Core Chatterbox TTS (install from GitHub)
git+https://github.com/resemble-ai/chatterbox.git

# Core dependencies for TTS functionality
# Matching your existing requirements for consistency
librosa>=0.10.0
safetensors>=0.4.0
soundfile>=0.12.1
pydub>=0.25.1
audiotsm>=0.1.2
praat-parselmouth>=0.4.3
requests>=2.31.0
PyYAML>=6.0
unidecode>=1.3.6
inflect>=6.0.0
tqdm>=4.65.0

# Chatterbox specific dependencies with pinned versions
# Same as your existing setup
conformer==0.3.2
diffusers==0.29.0
resemble-perth==1.0.1
transformers==4.46.3
onnx==1.16.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0

# FastAPI components (minimal - for compatibility with your existing code)
fastapi>=0.104.0
python-multipart>=0.0.6

# File handling
aiofiles>=23.0.0

# Additional dependencies that might be in your utils.py or engine.py
watchdog>=3.0.0